{
  "name": "monad-defi-agent-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for DeFi Agent Marketplace on Monad Testnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:monad": "hardhat run scripts/deploy.ts --network monadTestnet",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
